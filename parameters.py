# learning functions parameters in python
# a parameter is a variable used to define a particular characteristic of a function or model.


def greet_user(username):
    """Display a simple greeting."""

    print(f"Hello, {username}!")

    greet_user("Allan")
    greet_user("Maria")
    greet_user("John")
